## libft

<h3>Checklist before Submission</h3>

- Test functions with <code>-Wall -Wextra -Werror</code> flags
	- For Part 1, test with original functions and use <code>assert</code>
   - Check whether helper functions need to be defined as static functions
- Run <code>make</code>
- Check file names
- Check function names and declarations
	- For Part 1, check against manuals
- Check that only allowed functions are used e.g., no <code>printf</code>
- Check that header file has all the functions
- Check that header file has correct function prototypes
- Move files into the git folder
- Check that only files required are in folder (e.g., remove <code>a.exe</code>, <code>main.c</code>)
- Run norminette with <code>-R CheckForbiddenSourceHeader</code>
- Git add, commit, push
- Re-clone git and make sure that the files were pushed correctly!
